replicaCount: 3

image:
    repository: nginx
    tag: alpine
    pullPolicy: IfNotPresent

service:
    type: NodePort
    port: 80
    nodePort: 30080

# New custom values
environment:
    APP_ENV: "production"
    LOG_LEVEL: "info"

database:
    password: "MySuperStrongDbPass2026!"

